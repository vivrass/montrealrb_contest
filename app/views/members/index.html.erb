<h1>Listing members</h1>

<table>
  <% @members.each do |member| %>
    <tr>
      <td><%= image_tag member.gravatar_image %></td>
      <td><%= member.name %></td>
    </tr>
  <% end %>
</table>

<br />
<%= link_to 'Register', new_member_path %>
